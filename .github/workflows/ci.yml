name: Continuous Integration

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  #  test-javascript:
  #    name: JavaScript Tests
  #    runs-on: ubuntu-latest
  #
  #    steps:
  #      - name: Checkout
  #        id: checkout
  #        uses: actions/checkout@v4
  #
  #      - name: Setup Node.js
  #        id: setup-node
  #        uses: actions/setup-node@v4
  #        with:
  #          node-version-file: .node-version
  #          cache: npm
  #
  #      - name: Install Dependencies
  #        id: npm-ci
  #        run: npm ci
  #
  #      - name: Check Format
  #        id: npm-format-check
  #        run: npm run format:check
  #
  #      - name: Lint
  #        id: npm-lint
  #        run: npm run lint
  #
  #      - name: Test
  #        id: npm-ci-test
  #        run: npm run ci-test

  test-action:
    name: GitHub Actions Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v4

      - name: Test local action 'Wait for Response'
        id: waitForResponse
        uses: schwarzland/wait-for-response-action@main
        with:
          url: 'google.com'
          expected-http-status: '200'
          interval: '1000'
          timeout: '10'
          abort-at-timeout: 'false'

      - name: Print Output
        id: output
        run: |
          echo "${{ steps.waitForResponse.outputs.time }}"
          echo "${{ steps.waitForResponse.outputs.timeout-reached }}"
          echo "${{ steps.waitForResponse.outputs.desired-status }}"
          echo "${{ steps.waitForResponse.outputs.response }}"
